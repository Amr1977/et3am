# ET3AM-016: Donations Page Map Centering on User Location

**Status:** PENDING

## Description
Apply the same geolocation centering + blue animated marker that was done on the home page (ET3AM-012) to the donations page map (`DonationsMap.tsx`).

## Context from ET3AM-012
- Home.tsx now has: `userLocation` state, geolocation `useEffect`, `MapCenterUpdater` with `flyTo()`, blue pulsing marker with accuracy circle
- DonationsMap.tsx already has `userLocation` prop and `userLocationIcon` defined, but the donations page (`Donations.tsx`) doesn't pass `userLocation` to it
- The `DonationsMap` component also already supports rendering the user marker and circle when `userLocation` prop is provided

## What Needs to Be Done
1. In `Donations.tsx`: add geolocation `useEffect` + `userLocation` state
2. Pass `userLocation` to `<DonationsMap>` component
3. Map should `flyTo` user location when geolocation resolves

## Acceptance Criteria
- [ ] Donations page map centers on user location after geolocation resolves
- [ ] Blue animated pulsing marker shown at user location on donations map
- [ ] Falls back to default view if geolocation fails/denied
- [ ] Consistent behavior with home page map

## Relevant Files
- `frontend/src/pages/Donations.tsx`
- `frontend/src/components/DonationsMap.tsx` (already has userLocation prop support)

## Deployment
- [ ] Firebase Hosting
