# ET3AM-012: Home Page Map Centering on User Location

**Status:** DONE

## Description
Center the home page hero map on the user's current location with a blue animated marker when geolocation is available.

## Changes
- Added `userLocation` state + geolocation `useEffect` in `Home.tsx`
- Added `MapCenterUpdater` component using `useMap().flyTo()` for smooth animation
- Added blue pulsing user location marker with accuracy circle (reusing `userLocationIcon` pattern from `DonationsMap.tsx`)
- Also included marker popup fixes from previous session (DonationsMap.tsx, ClusterMap.tsx)

## Acceptance Criteria
- [x] Map centers on user location after geolocation resolves
- [x] Blue animated pulsing marker shown at user location
- [x] Map falls back to default Cairo center if geolocation fails/denied
- [x] Smooth flyTo animation when location is obtained

## Tests
- Backend: 68/68 PASS
- Frontend Build: PASS (198 modules)

## Deployment
- [x] Firebase Hosting (foodshare777)

## Files Modified
- `frontend/src/pages/Home.tsx` — Geolocation, MapCenterUpdater, user marker + circle
- `frontend/src/components/DonationsMap.tsx` — Marker popup fix (from previous session)
- `frontend/src/components/ClusterMap.tsx` — Marker popup fix (from previous session)
- `VERSION` — 1.14.1 → 1.15.0
